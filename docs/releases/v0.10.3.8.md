# ChatSheet v0.10.3.8

修复国内版 WorkBuddy 首次查询失败后，当天自动签到一直停在未知状态的问题。

- 未知或未完成状态不再作为当天完成结果复用。
- 面板打开期间，未确认状态每隔五分钟自动重新检查；签到成功后停止同日重查。
- 已提交过的领取只回查，不重复发送；切出国内模式后停止重查，国际版签到边界保持不变。

已通过签到缓存、多个面板并发、请求失败恢复、模式切换和跨日定向回归。2026-09-20 实机通过已有领取流程完成今日签到，并由官方接口回查确认。

安装前保存并完全退出 Excel/WPS，在项目目录运行 `powershell -File .\scripts\install.ps1 -Action install -SkipBuild`。重新打开后确认标题栏为 `0.10.3.8`。本版本未发布至 GitHub Release。
