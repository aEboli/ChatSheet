# 任务

- [x] 创建授权状态、模型目录、安全边界和未实现对话行为的 OpenSpec。
- [x] 新增 WorkBuddy ACP provider：路径发现、JSONL RPC、超时、回收、状态与模型解析。
- [x] 接入设置解析、`settings.get`、`models.list` 和授权模式的普通对话拒绝。
- [x] 更新设置页授权状态、模型对象目录展示、保存校验和只读字段过滤。
- [x] 增加 C# provider/错误映射测试和前端授权模式回归测试。
- [x] 完成 Release 构建、现有 Web 测试、差异检查，并确认不泄漏 WorkBuddy 凭据。
