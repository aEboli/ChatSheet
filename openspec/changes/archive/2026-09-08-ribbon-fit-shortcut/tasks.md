## 1. 功能区入口

- [x] 1.1 在 `Ribbon.xml` 的“诊断”之后加入“适配当前表”按钮，并绑定唯一回调名
- [x] 1.2 在 `ComAddIn` 与 `TaskPaneController` 中转发快捷动作，先显示面板并进入对话页

## 2. 可靠触发

- [x] 2.1 在 `TaskPaneControl` 中记录页面就绪状态，冷启动时排队一次适配动作
- [x] 2.2 页面加载完成后点击现有适配控件，复用当前对齐、操作卡片与撤销链路

## 3. 验证

- [x] 3.1 新增功能区按钮顺序、标签、回调与冷启动转发链路的静态测试
- [x] 3.2 运行适配相关面板测试、全部 Web 测试与 Release 构建
- [x] 3.3 严格校验并归档 OpenSpec 变更，确认中文内容和仓库差异无异常
