# model-capability-fallback

按模型实际具备的能力调整请求，而不是假定所有模型都能调用工具、都能看图。

- 不支持原生工具调用 → 改用文本指令协议，模型照样能读写工作簿；连指令块也写不
  对时退为顾问模式，并说明它这一轮只能给方案。
- 不支持视觉输入 → 配了中转模型就先把图转成文字描述再交给主模型；没配就去掉图
  片继续这一轮，并告知模型「用户附了图但你看不到」。

两类回退都自动探测、自动生效，探测结果按「连接 + 模型」缓存在本次面板会话内。
设置页可手动指定工具协议与视觉中转模型，跳过探测。
