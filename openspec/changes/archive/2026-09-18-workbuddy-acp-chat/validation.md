# 0.10.2 验证记录

验证日期：2026-09-18。

## 修复结果

- 原安装版本为 0.10.1.0，旧版仅支持授权目录，发送会返回未接入提示。
- 已通过 WorkBuddy 安装目录中的 CLI 接通 ACP 对话、选模型和模型探测。实机测试中发现逐次读取标准输出的方式可能停滞，改为持续事件读取并排队消费；正文、工具续传和取消均已验证。
- 模型目录保留安全倍率，设置页与对话选择器一致显示；模型 ID 仍是保存值。
- 标题栏为阿拉伯数字 0.10.2，保留楷体，无“版本”前缀。

## 验证

- Release 构建：0 警告、0 错误。
- 前端 30 个测试脚本通过，覆盖模型倍率缓存、同 ID 倍率更新、失效清理和版本显示。
- 完整工具测试：729 项通过、0 失败。
- ACP 进程夹具：6 项通过，覆盖中文、连续大通知、流事件、工具续传、系统更新、字符串权限请求、取消后进程回收、未登录错误。
- WorkBuddy 实机：读取授权目录成功；hy3 回复 `OK`；续传工具结果后回复 `ACP中文验证42`；模型探测判定 Available。
- 真实 Excel：AgentRunner 经一次审批，实际将临时工作簿 A1 写为 `ACP中文验证42`。测试工作簿关闭且未保存。
- 真实 WebView2：360 和 420 像素宽度均通过版本、倍率、弹层边界、按钮不换行检查，无横向溢出。截图位于 `artifacts/workbuddy-0.10.2/`。
- 旧选择器夹具曾被异步返回的真实授权目录覆盖；已在测试宿主隔离该请求，复测进程退出码为 0。
- `git diff --check` 通过；新增中文内容和截图显示正常。

## 安装

- 已执行 `scripts/install.ps1 -Action install` 和 `-Action diagnose`。
- 已安装程序集为 0.10.2.0；程序集与关键网页文件哈希均匹配构建产物。
- 32/64 位 COM 类和面板控件注册正常，Excel LoadBehavior=3，WebView2 与 .NET Framework 依赖正常。
- 原设置保留：Authorized / hy3 / Max。
- 旧程序备份：`C:\Users\Administrator\AppData\Local\ChatSheet\backups\0.10.1-20260918-122507`。
- 安装仅替换 `C:\Users\Administrator\AppData\Local\ChatSheet\app`。WPS 进程未占用载荷，安装正常完成；未结束用户进程。
