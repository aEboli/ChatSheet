## 1. 审计与方案确认
- [x] 识别审批、范围、缺失入口、步数和撤销边界。
- [x] 记录用户确认及工具调用开放要求。
- [x] 将方案收敛为专用工具加通用对象入口；同步规范、设计与验证边界。

## 2. 解除已有操作阻断
- [x] 以原有范围拒绝测试复现 5000 格拦截，更新为实际执行及分页断言。
- [x] 默认自动执行，实测通用对象调用不触发审批；保留显式审批回归。
- [x] 读取分页、分块写入与独立撤销预算；验证宽范围页边界及中止后的部分进度。
- [x] 单格文本可返回至 Excel 的 32767 字符上限，多区域宿主操作经对象入口开放。
- [x] 步数 0 持续执行；循环条件代码审查确认不受原 40 步限制，实测八次相同失败终止和取消边界。

## 3. 工具调用开放
- [x] 查询官方对象模型，提供宿主版本、工具入口及类别信息。
- [x] 实现结构化对象路径、属性、方法、对象引用、二维数组及缺省参数校验。
- [x] Excel/WPS 临时工作簿验证行列、格式、筛选、数据规则、命名区域、透视表及视图布局样本。
- [x] 原有工作表、表格、图表、合并、排序和撤销在完整回归中验证。
- [x] 记录本机 Excel Copy 失败、WPS 成功；不把失败反馈测试当成功能成功。
- [x] 同步原生/文本工具清单、系统提示、中文卡片及无法撤销说明。

## 4. 验收与交付
- [x] Release 构建，完整工具回归、定向 COM 检查和 Web 回归。
- [x] 更新本机安装，核对版本、哈希、注册及 Excel 加载项连接。
- [x] 本机设置保存并读回 Automatic 和 maxSteps=0，保留修改前备份。
- [x] 更新说明、中文编码检查与严格规范校验；明确未执行真实模型端到端验收及非穷举的宿主成员覆盖。
- [x] 同步正式规范并归档。