# 任务

- [x] 新增直接进程与 WMI 命名管道代理共用的 ACP 传输封装，并用 Job Object 管理代理生命周期。
- [x] 让模型探测、登录和对话统一使用传输封装。
- [x] 安装时创建并验证 WPS 可见的官方 CLI 硬链接桥接入口；独立安装器也支持 WMI 降级启动。
- [x] 增加 x86、代理传输、代理回收和路径桥接回归测试。
- [x] 在真实 WPS x86 中点击国际版登录，确认版本、WMI ACP、`external` 授权通知和默认浏览器均出现；当前账号未授权时模型探测返回明确状态，不再卡死。
- [x] 完整构建、网页与面板测试、安装、严格校验并归档 OpenSpec 变更。
