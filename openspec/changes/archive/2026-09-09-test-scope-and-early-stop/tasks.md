## 1. 探测调度提前停

- [x] 1.1 `ProbeManyAsync` 新增目标数参数（0 表示不提前停），返回本批结局
- [x] 1.2 达标检查放在拿到并发槽位之后、派发之前；已达标就归还槽位并跳出派发循环
- [x] 1.3 达标计数只认 `Available`，且在 `onResult` 之前置位、在归还槽位之前递增
- [x] 1.4 达标走「停止派发」：保留 `WhenAll` 与单飞闸门的结构，不 Cancel、不抛异常

## 2. 通道与结局字段

- [x] 2.1 `models.test.all` 读目标数并收拢非法值，非数字不得让整次运行不发生
- [x] 2.2 实发条数与可用个数由通道侧自己计数，取消路径上照样是真的
- [x] 2.3 回复与 `probe-progress` 带上目标数、可用个数、实发条数与结局；结局按状态判定，不看异常类型
- [x] 2.4 日志分三支：用户中止 / 达标（写出剩余未发条数）/ 全部测完

## 3. 范围菜单

- [x] 3.1 列头「测试」改为打开范围菜单，选中一档才发请求；键盘可打开可选中，无悬停设备行为一致
- [x] 3.2 每档写出上界（一律等于目录条数）、什么会让它提前结束、以及最少会发多少条
- [x] 3.3 菜单装不下时有明确让位规则，不被浮层静默裁掉；列头仍占一行
- [x] 3.4 两套主题都只走调色板变量；减少动效下仍读得出状态
- [x] 3.5 禁用原因补上「对话进行中」，不再只落在宿主日志里

## 4. 结局呈现

- [x] 4.1 运行中的分母改成目标数
- [x] 4.2 达标、用户中止、目标未达成而全测完，三者各有各的说法
- [x] 4.3 收尾之后迟到的推送不复活进度

## 5. 验证

- [x] 5.1 `BulkTestTests` 新增一组：真的少发了请求（数服务端连接数）、已派发的都拿到判定、闸门到整批结束才放开、达标与取消分得开、目标达不到时跑满全程
- [x] 5.2 `model-test-all.test.mjs` 改写「点击即发请求」那一段为「开菜单、选一档才发」，并补菜单与结局文案的断言；新节点补进 ids 表
- [x] 5.3 `PaneHarness --picker` 补菜单几何与主题断言，含窄栏与矮视口
- [x] 5.4 每条新断言配一次变异，确认会红
- [x] 5.5 跑全部 Web 测试、ToolTests 与 Release 构建
- [x] 5.6 写 `docs/changes` 记录、更新 README 能力表与验证命令
- [x] 5.7 严格校验并归档 OpenSpec 变更
